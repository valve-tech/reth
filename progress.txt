VALVE FORK — PROGRESS / FOLLOW-UP TASKS
=======================================

------------------------------------------------------------------------------
TASK: Firehose test coverage — remaining gaps after the v2.3 upgrade
STATUS: PARTIALLY DONE (added the feasible high-value guards; two items deferred)
OPENED: 2026-06-15
OWNER: valve
------------------------------------------------------------------------------

ADDED (2026-06-15, crates/firehose-tests/tests/):
  - setup_guards.rs::cancun_block_emits_beacon_root_system_call  (gap #2)
      Functional guard: a Cancun block with parentBeaconBlockRoot must produce a
      non-empty firehose `system_calls`. Fails loudly if the alloy-evm inspector
      patch is ever silently dropped (the exact regression we hit on this upgrade).
  - setup_guards.rs::alloy_evm_resolves_to_valve_fork           (gap #10)
      Structural guard: parses workspace Cargo.lock and asserts alloy-evm resolves
      to the valve-tech/evm fork, not unpatched crates.io.
  - pipeline_trace.rs::pipeline_executor_matches_golden          (gap #1, pipeline)
      Drives a block through FirehoseBlockExecutor::execute_and_trace_one (staged-sync
      path, process-wide tracer) and asserts byte-identical output to the golden.
      Covers the global-tracer lifecycle, the Executor<DB> impl (execute_and_trace_one
      / into_state / take_bal), and the deferred mark_verified flush — none of which
      the local-tracer prestate harness exercised.

DEFERRED (tracked here — not worth shipping brittle tests for):
  1. LIVE engine-API path: TreePayloadValidator::execute_and_trace_block
     (crates/engine/tree/src/tree/payload_validator.rs). This is the orchestration
     most heavily reworked on the v2.3 rebase (parallel-BAL dispatch, convert_to_block
     re-fit, 3-tuple->4-tuple adaptation, mark_verified). NOT covered.
     Why deferred: needs a fully-constructed TreePayloadValidator (heavy generics) and
     the engine-tree TestHarness is a private #[cfg(test)] type (not reusable from an
     integration-test crate). Worse, the firehose tracer is a process-wide singleton
     (init_tracer is once-per-process, no reset), so initializing it inside engine-tree's
     shared test binary would force EVERY block-validation test in that process onto the
     traced path and break them.
     Mitigation today: execute_and_trace_block shares the FirehoseWrappedExecutor core
     with run_wrapped_block (golden-tested) and FirehoseBlockExecutor (pipeline test),
     and otherwise mirrors the upstream execute_block (covered by the engine-tree suite).
     To do it properly: a dedicated engine-tree integration-test binary that (a) exposes
     a minimal block-validation entry point and (b) inits a buffer-backed global tracer,
     run in isolation (its own process / nextest).
  2. BAL (EIP-7928) x firehose dispatch. Firehose forces the sequential, non-BAL path
     (execute_and_trace_block always passes has_bal=false and the dispatch takes the
     tracer branch before parallel_bal_execution). No test confirms a BAL-eligible block
     still traces, nor that the non-firehose BAL path is unaffected.
     Why deferred: BAL requires the Amsterdam fork, which is not active on any chain we
     run (incl. PulseChain), so there is no realistic block to exercise it; and the
     dispatch lives in the validator (same scaffolding problem as #1). Revisit when
     Amsterdam/EIP-7928 is on the roadmap.
  DONE since (2026-06-15, batch 2):
    - crates/pulsechain/node/src/consensus.rs: 3 tests for the Shanghai-timestamp-gap
      validate_header logic (pre-fork blocks validate withdrawals_root against the
      Ethereum mainnet Shanghai timestamp).
    - crates/pulsechain/node/src/gas.rs: extracted apply_gas_margin + 4 unit tests
      (+20% margin, zero, truncation, saturating overflow).
  STILL worth adding later (lower priority):
    - otterscan ots_searchTransactions index-vs-full-scan EQUIVALENCE. BLOCKED on test
      scaffolding: MockEthProvider's AccountHistoryReader impl is a stub (both methods
      return Ok(vec![])), so the index path finds nothing in tests. Needs either (a)
      enhancing MockEthProvider to derive account-change blocks from its stored
      blocks/changesets (shared crate — may affect other tests), or (b) a DB-backed
      integration test with a populated changeset index. The pure pagination helper
      block_transaction_page_range is already unit-tested.
    - End-to-end firehose golden cases for a block WITH withdrawals, contract creation,
      selfdestruct, logs, multi-tx ordinal ordering, EIP-7702 set-code, and a
      PrimordialPulse fork block. Needs new prestate fixtures; goldens would be
      self-generated (regression locks) unless produced from a geth oracle like the
      existing two cases.
    - PulseChain estimate_gas end-to-end (margin applied + block_overrides forwarded
      through the RPC stack): needs a mock EthApiServer + jsonrpsee invocation. The
      margin arithmetic itself is now unit-tested via apply_gas_margin.

------------------------------------------------------------------------------
TASK: Track upstream reth releases / keep the fork current
STATUS: **v2.4.0 BUMP DONE** (2026-07-15) — branch feature/update-to-reth-2.4,
        tag v2.4.0-pulse. 132/132 valve tests pass. NOT yet deployed.
OPENED: 2026-07-15
OWNER: valve
------------------------------------------------------------------------------

UPSTREAM STATE (checked 2026-07-15)
  - paradigmxyz/reth **v2.4.0** released 2026-07-14: 206 commits, 398 files,
    +22,470/-18,004. Release notes:
    https://github.com/paradigmxyz/reth/releases/tag/v2.4.0
  - We rebased our 10 curated valve commits onto v2.4.0 (true `git rebase
    --onto v2.4.0 v2.3.0`; our history is clean enough that no squash-merge was
    needed). Only 3 code conflicts, all in files the release notes predicted.
  - Previous base v2.3.0 (tagged 2026-06-10) lives on at
    feature/update-to-reth-2.3 / tag v2.3.0-pulse; safety tag
    pre-2.4-rebase-backup.

!! NEW BUILD-TOOLCHAIN REQUIREMENT IN v2.4.0 (deploy-affecting) !!
  v2.4.0 ADDED `jit` and `gmp` to bin/reth's DEFAULT features (v2.3.0 had
  neither). That means a default `cargo build -p reth` now needs:
    - **LLVM** (revmc JIT -> revmc-llvm -> inkwell -> llvm-sys), and
    - **GMP/MPFR** (gmp-mpfr-sys, builds via make).
  Firehose-box provisioning must install these, OR build with
  `--no-default-features` + the v2.3-era feature set:
    jemalloc,otlp,otlp-logs,reth-revm/portable,js-tracer,keccak-cache-global,
    asm-keccak,min-trace-logs
  (verified: the binary compiles clean with that set on v2.4.0, no LLVM needed).
  NOTE the `jit` feature only COMPILES the JIT in; it stays off at runtime
  unless `--jit.enabled` is passed. Do NOT pass it on firehose boxes until the
  inspector-vs-JIT question below is settled.

WHAT THE v2.4.0 BUMP REQUIRED (done 2026-07-15 — kept for the next bump)

  1. alloy-evm patch — DONE.
     v2.4.0 bumps alloy-evm 0.36.0 -> 0.37.0 (its lockfile resolves 0.37.1),
     revm 40.0.3 -> 41.0.0, alloy-consensus 2.0.5 -> 2.1.1, alloy-eip7928
     0.4.0 -> 0.4.5, revm-inspectors -> 0.41.0.
     Our existing patch (valve/v0.36.0) would STOP version-matching ^0.37.0, so
     cargo would silently drop it and firehose would stop tracing system calls
     (the 2.2->2.3 regression, again).
     -> Pre-built **valve-tech/evm branch `valve/v0.37.1`** = upstream alloy-evm
        tag v0.37.1 + the cherry-picked "Enable system call to be traced by
        inspector" commit. Cherry-picked clean; `cargo check -p alloy-evm` passes
        against revm 41.0.0 (`inspect_system_call_with_caller` still exists there).
     -> On the bump: repoint reth Cargo.toml [patch.crates-io] alloy-evm to
        `branch = "valve/v0.37.1"`, then `cargo update -p alloy-evm` and confirm
        Cargo.lock's alloy-evm `source` is the valve fork.
     -> The guards added 2026-06-15 (firehose-tests/tests/setup_guards.rs) will
        fail loudly if this is forgotten — that is exactly what they are for.

  2. Breaking changes that landed on code we modify — all RESOLVED:
     - payload_validator: v2.4.0 moved the execution state hook out of
       `PayloadHandle` into an explicit param sourced from
       `state_root_job.take_execution_hook()`, and replaced
       `payload_processor.executor()` with `self.runtime`. Our
       `execute_and_trace_block` (firehose twin of `execute_block`) now takes a
       `state_hook` param and spawns via `self.runtime`; the dispatch forwards
       `execution_state_hook`.
     - payload_validator: the `changeset_provider` acquisition was removed (the
       deferred trie task now takes `changed_paths`). Our firehose
       `mark_verified` flush was kept; the removed block dropped.
     - chain-state/deferred_trie.rs: DELETED upstream by the state-root rework,
       so our rayon-worker debug_assert went with it (it guarded upstream code,
       not a valve feature — nothing lost).
     - otterscan: revm 41 dropped the `revm_primitives` re-export; `FixedBytes`
       now imports from `alloy_primitives`.
     - .github/workflows/pr-audit.yml deleted upstream -> dropped our disabled copy.
     - `--engine.legacy-state-root` dropped -> `--engine.state-root-fallback`
       (deploy flag check if anything passes it).

  3. Worth watching (not blocking):
     - revmc JIT EVM (#23230). The `jit` FEATURE is now on by default in
       bin/reth (so it compiles in, and drags LLVM into the build), but the JIT
       only runs when `--jit.enabled` is passed. OPEN QUESTION: does a JIT'd EVM
       still fire Inspector call hooks? If not, enabling it would silently gut
       firehose tracing — the same class of bug as the system-call patch. Do NOT
       pass `--jit.enabled` on firehose boxes until this is probed.
     - Goldens did NOT need regenerating on this bump: revm 41 kept
       SYSTEM_CALL_GAS_LIMIT at 31,566,720, so the prestate goldens matched
       byte-for-byte.
     - Amsterdam / EIP-7928 BAL work continues (BAL validation in
       flashbots_validateBuilderSubmissionV6, BAL-capable download clients).
       Brings the deferred "BAL x firehose" test closer to being testable.

RECIPE — new alloy-evm patch branch per reth bump
  1. Find the alloy-evm version reth resolves to:
       git show <reth-tag>:Cargo.lock | grep -A2 'name = "alloy-evm"'
  2. In ../evm (valve-tech/evm):
       git fetch upstream --tags
       git checkout -b valve/vX.Y.Z vX.Y.Z
       git cherry-pick 95a512e     # "Enable system call to be traced by inspector"
       cargo check -p alloy-evm
       git push -u origin valve/vX.Y.Z
  3. Repoint reth Cargo.toml [patch.crates-io] alloy-evm at the new branch.
  4. `cargo test -p reth-firehose-tests` — the setup guards verify the patch is live.

------------------------------------------------------------------------------
TASK: Re-review whether the alloy-evm fork can be folded back to upstream
STATUS: IN PROGRESS (parked — keeping the minimal fork; re-reviewed 2026-07-15)
OPENED: 2026-06-15 (during the reth v2.2 -> v2.3 upgrade)
RE-REVIEW: at the next reth upstream bump, OR by 2026-09-15 — whichever is first
  - 2026-07-15 re-review (trigger: v2.4.0 released). VERDICT: KEEP THE FORK.
    alloy-rs/evm#323 is still OPEN and untouched since 2026-03-31, and upstream
    alloy-evm v0.37.0 AND v0.37.1 still call the un-inspected
    `system_call_with_caller`. No released upstream version carries the fix, so
    the patch cannot be dropped. Next re-review: the next reth bump after v2.4.0.
OWNER: valve
------------------------------------------------------------------------------

WHY THIS EXISTS
---------------
Firehose must trace pre-execution system calls (EIP-4788 beacon-root,
EIP-2935 block-hash history). Stock alloy-evm does NOT route system calls
through the Inspector: `EthEvm::transact_system_call` (crates/evm/src/eth/mod.rs)
calls `self.inner.system_call_with_caller(...)` unconditionally. So with stock
alloy-evm the firehose inspector never sees the system-call frame.

To fix this the valve fork patches alloy-evm via reth's `[patch.crates-io]` to a
fork that adds:

    fn transact_system_call(&mut self, caller, contract, data) {
        if self.inspect {
            self.inner.inspect_system_call_with_caller(caller, contract, data)
        } else {
            self.inner.system_call_with_caller(caller, contract, data)
        }
    }
    // + import revm::InspectSystemCallEvm

CURRENT STATE (as of 2026-06-15, reth v2.3.0 upgrade)
-----------------------------------------------------
- reth v2.3.0 bumped the workspace alloy-evm requirement to 0.36.
- The OLD patch pointed at streamingfast/evm branch `sf/v0.34.0`. After the bump
  the version no longer matched 0.36, so cargo SILENTLY ignored the patch and
  built against unpatched crates.io alloy-evm 0.36. Result: firehose emitted
  `system_calls: []` and every event ordinal shifted down by 2. Caught by the
  golden tests in crates/firehose-tests/tests/prestate.rs (nop_transfer,
  storage_sstore_oog).
- FIX: created valve-tech/evm branch `valve/v0.36.0` = upstream alloy-evm tag
  v0.36.0 + the cherry-picked "Enable system call to be traced by inspector"
  commit. reth's [patch.crates-io] alloy-evm now points there. Committed in
  reth e5e33ed15 "fix(firehose): restore system-call tracing via valve
  alloy-evm 0.36 patch".
- The valve/v0.36.0 fork is MINIMAL: the upstream release tag + exactly one
  commit. Nothing else diverges.
- NOTE on gas: revm 20.x (used by alloy-evm 0.36) sets
  SYSTEM_CALL_GAS_LIMIT = 30,000,000 + EIP-7928 BAL state-gas reserve
  = 31,566,720. geth reports 30,000,000. This is a parity nuance on the
  system-call frame's `gas_limit` field only (gas_consumed is unaffected). The
  prestate goldens were regenerated to match revm 20.x.

WHY WE CAN'T FOLD BACK TO UPSTREAM YET
--------------------------------------
- Upstream PR: alloy-rs/evm#323 "Enable system call to be traced by inspector"
  (StreamingFast / Matthieu Vachon) — this is the SAME change our fork carries.
- As of 2026-06-15 #323 is OPEN, unmerged. The latest upstream release is
  v0.37.0 and `upstream/main` STILL has the un-inspected system_call_with_caller.
  There is no upstream version we can switch to.
- Merge is blocked on a maintainer concern (klkvr, alloy member):
    "i think we should make sure that any reth rpc callsites explicitly disable
     inspection before pre/post execution hooks before making this change"
  Rationale: inspecting system calls is a HIDDEN behavioral change for callers
  that don't want them. geth does NOT trace system calls in debug_traceBlock
  (it builds the pre/post EVM without a tracer). Landing this upstream first
  requires auditing reth's RPC callsites (e.g.
  crates/rpc/rpc-eth-api/src/helpers/trace.rs and likely others) to explicitly
  disable_inspector around apply_pre/post_execution_changes. That is cross-repo
  work into reth itself, not a one-liner.

KNOWN SIDE-EFFECT OF OUR PATCH (the maintainer's concern, applied to our build)
------------------------------------------------------------------------------
Because our patch inspects system calls whenever `self.inspect` is set, valve's
debug_traceBlock / trace_* RPC endpoints will ALSO surface the EIP-4788/2935
system calls — a divergence from geth on those endpoints. Correct/required for
firehose; possibly over-tracing for the debug RPC. Has not broken anything
(122/122 valve regression tests pass), but it is exactly why upstream wants the
callsite guards before merging.

WHAT TO CHECK AT RE-REVIEW
--------------------------
1. Has alloy-rs/evm#323 (or an equivalent) merged?
       gh pr view 323 --repo alloy-rs/evm --json state,mergedAt
2. Has a RELEASED alloy-evm version shipped the change? Check the latest tag's
   crates/evm/src/eth/mod.rs transact_system_call for an `if self.inspect` /
   inspect_system_call_with_caller branch.
3. Has reth bumped its workspace alloy-evm requirement to a version that
   includes the change?
   If all three -> drop the [patch.crates-io] alloy-evm entry from reth's
   Cargo.toml, delete the valve-tech/evm valve/v0.* branches, re-run the
   prestate golden tests, done.
4. If #323 is still stalled and folding back matters: scope the reth RPC
   callsite-guarding work the maintainer asked for, push it to #323 (or a fresh
   PR) to unblock the merge.

REFERENCES
----------
- reth fix commit:        e5e33ed15
- reth branch/tag:        feature/update-to-reth-2.3 / v2.3.0-pulse
- alloy-evm fork:         github.com/valve-tech/evm  branch valve/v0.36.0
  (= upstream v0.36.0 + 1 commit: "Enable system call to be traced by inspector")
- upstream PR:            github.com/alloy-rs/evm/pull/323 (OPEN)
- patch declaration:      reth Cargo.toml [patch.crates-io] alloy-evm
- failing/regenerated:    crates/firehose-tests/tests/cases/{nop_transfer,
                          storage_sstore_oog}/*.binpb
- the patched method:     evm/crates/evm/src/eth/mod.rs::transact_system_call
- revm gas constant:      revm-handler 20.x SYSTEM_CALL_GAS_LIMIT (= 31,566,720)
