[package]
name = "reth-firehose"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
homepage.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
firehose-tracer.workspace = true

# reth
reth-chainspec.workspace = true
reth-ethereum-forks.workspace = true
reth-evm.workspace = true
reth-execution-types.workspace = true
reth-exex.workspace = true
reth-node-api.workspace = true
reth-ethereum-primitives.workspace = true
reth-primitives-traits.workspace = true
reth-provider.workspace = true
reth-revm.workspace = true
reth-tracing.workspace = true

# PulseChain-specific (intentional cross-crate dep): the firehose wrapper layer
# emits FIRE BLOCK events for the PrimordialPulse state transition by walking
# the spec data (sacrifice credits binary, deposit-contract bytecode + initial
# storage, treasury allocation). That data lives in `reth-pulsechain-forks`,
# the lowest-level PulseChain crate, which has no dependency on firehose so
# this edge is acyclic. See `FIXME(firehose-primordialpulse)` history.
reth-pulsechain-forks.workspace = true

# alloy
alloy-consensus.workspace = true
alloy-eips.workspace = true
alloy-evm.workspace = true
alloy-genesis.workspace = true
alloy-primitives.workspace = true
alloy-rlp.workspace = true

# std
eyre.workspace = true
futures.workspace = true
rayon = { workspace = true, optional = true }
